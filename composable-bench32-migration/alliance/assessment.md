# Assessment

Currently, the data of this module is not created yet in this blockchain. There are only ValidatorAddress in genesis file. But these the validator addresses is converted into accAddr type and then used for key storage. 
Then the `alliance` module `is not being affected` by the migration process. 

